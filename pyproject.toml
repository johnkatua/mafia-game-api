[tool.poetry]
name = "mafia-game-api"
version = "0.1.0"
description = ""
authors = ["johnkatua <johnkatua99@gmail.com>"]
readme = "README.md"
packages = [{include = "mafia_game_api"}]

[tool.poetry.dependencies]
python = "^3.8"
fastapi = {extras = ["standard"], version = "^0.115.8"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
